# AutoLabs Platform - Детальная Документация

## Дата создания
2025-11-29

---

## О платформе

**AutoLabs** - защищённая микросервисная платформа для управления лабораторными работами по информационной безопасности.

### Ключевые особенности:
- **Микросервисная архитектура** на базе Kubernetes
- **Динамическое развёртывание** изолированных окружений для студентов
- **Гибкая конфигурация** лабораторных работ через YAML
- **Централизованная авторизация** с ролевой моделью доступа
- **Асинхронная обработка** задач через очереди RabbitMQ
- **CI/CD** автоматизация через GitLab

---

## Архитектурные принципы

### 1. Один Backend - множество задач
**Backend Platform** - единый FastAPI сервис, который:
- Обрабатывает запросы от пользователей (REST API)
- Управляет бизнес-логикой платформы (курсы, студенты, задания)
- Ставит задачи в очереди RabbitMQ для асинхронной обработки
- Получает результаты выполнения задач

**Celery Workers** - асинхронные исполнители, которые:
- Выполняют тяжёлые операции (создание лабораторных работ в K8s)
- Управляют Kubernetes API (создание Pods, Services, NetworkPolicies)
- Архивируют данные студентов в Minio
- Отправляют результаты обратно в очереди

**Преимущества:**
- ✅ Простота архитектуры (нет лишних прослоек)
- ✅ Асинхронность (пользователи не ждут долгих операций)
- ✅ Масштабируемость (можно добавлять Celery Workers независимо)

---

### 2. GitLab - единый источник образов

**Структура GitLab:**
```
GitLab Instance (registry.gitlab.local)
│
├── Group: autolabs/platform/          # Образы платформы
│   ├── frontend                        # React/Vue приложение
│   ├── backend-platform                # FastAPI Backend
│   └── celery-workers                  # Celery исполнители (тот же код что backend)
│
└── Group: autolabs/labs/               # Образы лабораторных работ
    ├── sql-injection                   # Лаба: SQL Injection
    ├── network-pentest                 # Лаба: Network Penetration Testing
    ├── web-vulnerabilities             # Лаба: Web Application Vulnerabilities
    └── malware-analysis                # Лаба: Malware Analysis
```

**Процесс:**
1. Разработчик пушит код → GitLab CI билдит образ → Registry
2. Преподаватель создаёт Dockerfile лабы → GitLab CI билдит → Registry
3. Helm/Celery Workers используют образы из Registry для деплоя

**Разграничение:**
- `autolabs/platform/*` - доступ только для команды разработки
- `autolabs/labs/*` - доступ для преподавателей (с валидацией безопасности)

---

### 3. Helm - декларативный деплой платформы

**Helm Charts** используются для:
- Развёртывания Backend Platform в K8s
- Развёртывания Frontend в K8s
- Установки Zitadel (авторизация)
- Установки RabbitMQ (очереди)
- Настройки Nginx Ingress Controller

**Процесс деплоя:**
```bash
# GitLab CI/CD пайплайн
helm upgrade --install backend-platform ./helm/backend-platform \
  -f values-dev.yaml \
  --set image.tag=$CI_COMMIT_SHA
```

**НЕ используется для:**
- Лабораторных работ (они создаются динамически через Celery Workers + Python K8s Client)

---

### 4. Очереди RabbitMQ - асинхронная коммуникация

**Потоки сообщений:**

```
Backend Platform (FastAPI)
        ↓
    [Задачи в очередь]
        ↓
    RabbitMQ Queues:
    ├── lab.create          # Создание лабораторных работ
    ├── lab.delete          # Удаление лабораторных работ
    └── lab.archive         # Архивация volumes
        ↓
    Celery Workers
    ├── Создание Pods в K8s
    ├── Настройка изоляции
    └── Архивация в Minio
        ↓
    [Результаты в очередь]
        ↓
    RabbitMQ Queues:
    ├── lab.results         # Результаты создания
    └── notifications       # Уведомления студентам
        ↓
    Backend Platform
    ├── Обновление PostgreSQL (статусы)
    ├── Отправка WebSocket (real-time уведомления Frontend)
    └── Сохранение credentials (SSH доступ для студентов)
```

---

### 5. Zitadel - централизованная авторизация

**Роли и пермишаны:**

| Роль | Права (Permissions) |
|------|---------------------|
| **admin** | Полный доступ ко всей платформе, управление пользователями, настройка системы |
| **teacher** | Создание курсов, управление лабораторными работами, просмотр прогресса студентов, оценивание |
| **student** | Просмотр назначенных курсов, выполнение лабораторных работ, просмотр своего прогресса |
| **viewer** | Только чтение (для гостевого доступа, демонстраций) |

**Процесс авторизации:**
```
1. Пользователь → Frontend → Zitadel (логин)
2. Zitadel → JWT токен (содержит роль и пермишаны)
3. Frontend → Backend Platform (запрос + JWT токен в заголовке)
4. Backend Platform → Валидация токена (проверка подписи, срока действия)
5. Backend Platform → Проверка пермишанов (может ли эта роль выполнить действие?)
6. Backend Platform → Разрешить/Запретить операцию
```

**Пример пермишанов:**
```
teacher может:
- POST /api/v1/courses          (создать курс)
- POST /api/v1/labs/{id}/start  (запустить лабу)
- GET  /api/v1/students/progress (просмотр прогресса)

student может:
- GET  /api/v1/courses           (просмотр курсов)
- GET  /api/v1/labs/{id}/access  (получить SSH credentials)
- POST /api/v1/labs/{id}/submit  (отправить результат)

student НЕ может:
- POST /api/v1/labs/{id}/start   (запустить лабу) → 403 Forbidden
```

---

## Основные компоненты платформы

### Инфраструктура (Proxmox VMs)
- **VM: GitLab** - CI/CD, Container Registry, автоматизация
- **VM: Database** - PostgreSQL, Redis, Minio (централизованное хранилище)
- **VM: Kubernetes Cluster** - оркестрация приложений

### Приложения (Kubernetes)
- **Backend Platform** - REST API, бизнес-логика, постановка задач
- **Celery Workers** - асинхронные K8s операции
- **Frontend** - веб-интерфейс (React/Vue)
- **Zitadel** - авторизация и управление пользователями
- **RabbitMQ** - очереди сообщений
- **Nginx Ingress** - HTTP маршрутизация

### Данные
- **PostgreSQL** - метаданные платформы и лабораторных работ
- **Redis** - кэш, сессии, rate limiting
- **Minio** - конфигурации лабораторных работ, архивы, статичные файлы

---

## Структура документации

### 📁 01-infrastructure/
**Описание физической и виртуальной инфраструктуры**

Планируемые документы:
- `proxmox-setup.md` - Настройка Proxmox гипервизора
- `vm-gitlab.md` - Конфигурация VM для GitLab
- `vm-database.md` - Конфигурация VM для баз данных
- `vm-kubernetes.md` - Конфигурация VM для K8s кластера
- `network-architecture.md` - Сетевая архитектура, firewall, VPN
- `terraform-configs.md` - Terraform конфигурации для создания VMs

**Схемы:**
- `infrastructure-topology.drawio` - Топология инфраструктуры
- `network-diagram.drawio` - Сетевая схема

---

### 📁 02-backend-platform/
**Описание Backend Platform (FastAPI сервиса)**

Планируемые документы:
- `architecture.md` - Архитектура Backend Platform
- `api-endpoints.md` - Описание REST API endpoints
- `database-models.md` - SQLAlchemy модели (PostgreSQL)
- `authentication.md` - Интеграция с Zitadel, валидация JWT
- `permissions.md` - Проверка пермишанов, RBAC логика
- `queue-management.md` - Постановка задач в RabbitMQ
- `deployment.md` - Helm chart для деплоя Backend Platform

**Схемы:**
- `api-flow.drawio` - Поток обработки API запросов
- `db-schema.drawio` - Схема базы данных

---

### 📁 03-celery-workers/
**Описание Celery Workers (асинхронные исполнители)**

Планируемые документы:
- `architecture.md` - Архитектура Celery Workers
- `tasks.md` - Описание всех Celery задач
- `k8s-operations.md` - Операции с Kubernetes API
- `volume-archiving.md` - Архивация volumes в Minio
- `error-handling.md` - Обработка ошибок, retry логика
- `monitoring.md` - Мониторинг выполнения задач

**Схемы:**
- `task-flow.drawio` - Поток выполнения задач
- `k8s-interaction.drawio` - Взаимодействие с K8s API

---

### 📁 04-gitlab-cicd/
**Описание GitLab CI/CD и Container Registry**

Планируемые документы:
- `gitlab-setup.md` - Настройка GitLab инстанции
- `registry-structure.md` - Структура Container Registry (группы, проекты)
- `platform-pipelines.md` - CI/CD пайплайны для образов платформы
- `lab-pipelines.md` - CI/CD пайплайны для образов лабораторных работ
- `security-scanning.md` - Сканирование образов на уязвимости
- `deployment-process.md` - Процесс деплоя в K8s через Helm

**Схемы:**
- `cicd-flow.drawio` - Поток CI/CD от commit до production
- `registry-groups.drawio` - Структура групп в GitLab

---

### 📁 05-kubernetes/
**Описание Kubernetes кластера и ресурсов**

Планируемые документы:
- `cluster-setup.md` - Настройка K8s кластера (single-node / multi-node)
- `namespaces.md` - Описание всех namespace и их назначение
- `platform-namespace.md` - Детали namespace: platform (Frontend, Backend)
- `auth-namespace.md` - Детали namespace: auth (Zitadel)
- `messaging-namespace.md` - Детали namespace: messaging (RabbitMQ)
- `labs-namespace.md` - Детали namespace: labs (динамические ресурсы)
- `ingress-nginx.md` - Настройка Nginx Ingress Controller
- `network-policies.md` - Network Policies для изоляции
- `persistent-volumes.md` - PersistentVolumes и StorageClass

**Схемы:**
- `namespace-topology.drawio` - Топология namespace
- `network-policies.drawio` - Визуализация Network Policies

---

### 📁 06-authorization/
**Описание системы авторизации (Zitadel)**

Планируемые документы:
- `zitadel-setup.md` - Настройка Zitadel
- `roles-permissions.md` - Описание ролей и пермишанов
- `oauth2-flow.md` - OAuth2/OIDC процесс аутентификации
- `jwt-validation.md` - Валидация JWT токенов в Backend Platform
- `user-management.md` - Управление пользователями
- `multi-factor-auth.md` - Настройка MFA (опционально)

**Схемы:**
- `auth-flow.drawio` - Поток авторизации (от логина до API запроса)
- `roles-hierarchy.drawio` - Иерархия ролей и пермишанов

---

### 📁 07-lab-deployments/
**Описание процесса развёртывания лабораторных работ**

Планируемые документы:
- `lab-lifecycle.md` - Полный жизненный цикл лабораторной работы
- `yaml-config-format.md` - Формат YAML конфигураций для преподавателей
- `deployment-modes.md` - Режимы развёртывания (per student, per group, shared)
- `isolation-strategies.md` - Стратегии изоляции (network policies, resource quotas)
- `ssh-access.md` - Настройка SSH доступа для студентов
- `volume-management.md` - Управление PVC и архивация в Minio
- `multi-container-labs.md` - Развёртывание лаб с несколькими контейнерами

**Схемы:**
- `lab-lifecycle.drawio` - Диаграмма жизненного цикла
- `deployment-flow.drawio` - Поток развёртывания от запроса до доступа студента
- `multi-container-example.drawio` - Пример multi-container лабораторной работы

---

### 📁 08-network-flows/
**Описание сетевых потоков и очередей**

Планируемые документы:
- `rabbitmq-queues.md` - Описание всех очередей RabbitMQ
- `task-flow.md` - Поток задач (от Backend Platform до Celery Workers)
- `result-flow.md` - Поток результатов (от Celery Workers обратно)
- `websocket-notifications.md` - Real-time уведомления через WebSocket
- `external-access.md` - Внешний доступ пользователей (Nginx → Ingress → Services)
- `internal-communication.md` - Внутренняя коммуникация между компонентами

**Схемы:**
- `rabbitmq-topology.drawio` - Топология очередей RabbitMQ
- `full-request-flow.drawio` - Полный поток запроса (от пользователя до K8s pod)
- `async-task-flow.drawio` - Асинхронный поток задач

---

## Roadmap документирования

### Фаза 1: Критичные компоненты (Приоритет)
- [x] Общий README (текущий файл)
- [ ] `01-infrastructure/network-architecture.md` - Сетевая архитектура
- [ ] `02-backend-platform/architecture.md` - Архитектура Backend Platform
- [ ] `03-celery-workers/tasks.md` - Celery задачи
- [ ] `04-gitlab-cicd/registry-structure.md` - Структура GitLab Registry
- [ ] `06-authorization/roles-permissions.md` - Роли и пермишаны
- [ ] `07-lab-deployments/lab-lifecycle.md` - Жизненный цикл лабораторных работ
- [ ] `08-network-flows/rabbitmq-queues.md` - Очереди RabbitMQ

### Фаза 2: Детализация компонентов
- [ ] Все документы в `05-kubernetes/`
- [ ] Все схемы `.drawio` для каждой секции
- [ ] API документация (OpenAPI/Swagger)

### Фаза 3: Операционная документация
- [ ] Руководства по установке (step-by-step)
- [ ] Troubleshooting гайды
- [ ] Мониторинг и алертинг
- [ ] Backup и восстановление

---

## Ключевые изменения от предыдущих версий

### ✅ Один Backend вместо двух
**Было:** Backend Platform + Backend Infrastructure (два отдельных FastAPI сервиса)

**Стало:** Backend Platform + Celery Workers (один сервис, асинхронная обработка)

**Причина:** Упрощение архитектуры, устранение избыточной прослойки

---

### ✅ GitLab с разграничением по группам
**Добавлено:**
- Чёткое разделение: `autolabs/platform/` и `autolabs/labs/`
- Контроль доступа: разработчики vs преподаватели
- Связь лабораторных работ с образами из GitLab Registry

---

### ✅ Детализация очередей RabbitMQ
**Добавлено:**
- Явное описание потоков задач и результатов
- Схемы взаимодействия Backend Platform ↔ RabbitMQ ↔ Celery Workers

---

### ✅ Акцент на авторизацию и пермишаны
**Добавлено:**
- Детальное описание ролей (admin, teacher, student, viewer)
- Процесс проверки пермишанов при каждом API запросе
- Связь с Zitadel для централизованного управления доступом

---

## Соглашения о документации

### Формат файлов
- **Текстовая документация:** Markdown (`.md`)
- **Схемы:** Draw.io (`.drawio`)
- **Конфигурации:** YAML (`.yaml`)

### Структура документа
1. **Заголовок** (H1)
2. **Дата создания / обновления**
3. **Введение** (краткое описание)
4. **Основное содержание** (разделы H2, H3)
5. **Примеры** (код, конфигурации)
6. **Ссылки** (на связанные документы)

### Именование файлов
- Lowercase с дефисами: `lab-lifecycle.md`, `network-architecture.md`
- Схемы с суффиксом: `auth-flow.drawio`

---

## Контакты и участие

Документация разрабатывается итеративно по мере реализации платформы.

**Для вопросов и предложений:**
- GitHub Issues: [AutoLabs Issues](https://github.com/Artemka2031/AutoLabs/issues)
- Документация постоянно обновляется по мере развития проекта

---

**Версия документации:** 1.0
**Дата последнего обновления:** 2025-11-29
